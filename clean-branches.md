In local .gitconfig, add
```
[alias]
	clear = fetch --prune
	flush = removed-branches --prune
```

Remember to ``npm install -g git-removed-branches``

https://www.npmjs.com/package/git-removed-branches
